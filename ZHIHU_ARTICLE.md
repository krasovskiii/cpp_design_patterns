# 从 C++ 转金融工程，基本功夯实之设计模式

> 本文记录了我整理一套 C++ 设计模式教程的过程和思考，供想从 C++ 转向金融工程方向的朋友参考。它既是一篇项目总结，也是一份使用说明。观点难免有局限，欢迎指正。

---

## 一、为什么要写这套教程

先说我的困惑。很多写 C++ 的同行应该都有类似的经历：语法、STL、模板都学得差不多，也能应付算法题，可一旦去看真实的金融系统业务代码，还是觉得吃力。

这种吃力不是语法层面的，而是架构层面的。举个例子：一个期权定价引擎，怎么把 BS 解析解、Monte Carlo、PDE 有限差分几种算法组织在一起，让它们可以随时替换？一个订单管理系统，一笔订单从提交到成交要经过哪些状态，状态之间怎么转换？行情数据来了，怎么通知到几十个策略模块而不至于耦合过紧？

这些问题，设计模式确实是现成的回答框架。`Strategy` 描述算法替换，`State` 描述状态机，`Observer` 描述一对多的通知机制。学懂这些模式，再回头读金融系统的源码，会顺畅很多。

但我发现市面上的教程有一个共性的短板：**例子大多是文本编辑器、图形界面这类场景**。不是说这些例子不对，而是它们和金融工程离得太远，读者学完之后，很难把「模式」和「系统」对应起来。

所以这套教程做的事其实很朴素：

1. 在每个设计模式的讲解里，都标注了对应的金融工程应用场景——比如定价模型切换、订单状态机、行情订阅。
2. 从金融工程的实际应用频率出发，把 22 个 GoF 模式排了个学习优先级，分成四个梯队，而不是平铺直叙地逐个讲。

需要说明的是，这套教程的代码主体并非我原创，而是来自 GitHub 上的开源仓库 [mpavezb/cpp_design_patterns](https://github.com/mpavezb/cpp_design_patterns/)，它本身基于 Udemy 的《Design Patterns in Modern C++》课程。我是在这份代码的基础上，补充了金融工程应用场景注释、重排了学习优先级，并新增了少量示例代码；原作者的代码框架与示例会尽量保留并标注来源。具体每一项改动，都在仓库 README 的「来源与代码改动说明」一节里做了逐条列出。

## 二、C++ 在金融工程里到底处在什么位置

先回答一个很多人关心的问题：C++ 转金融工程，方向到底可不可行？

需要先澄清一点：金融工程是个很宽泛的领域，里面方向很多。C++ 的用武之地，主要集中在**对性能敏感、对延迟有硬性要求的系统**，比如：

- **交易/行情系统**：行情撮合、订单路由、风控网关这类路径，通常要求微秒甚至纳秒级延迟，Python 在这个层面基本无力承担，C++ 是主流选择。
- **定价与风险引擎**：衍生品定价、风险度量，很多机构会用 C++ 写核心库。现代 C++ 的模板与值语义在这里能发挥性能优势。
- **底层基础设施**：FIX 协议网关、交易接口封装、内存/事件驱动的中间件等。

但也要客观看待：金融工程里同样有大量偏研究、偏数据分析的工作，Python 在这些方向反而是主流。所以更准确的说法是——**C++ 是金融工程中「低延迟与高性能系统」方向的重要技能，但它不是通往金融工程的唯一道路，也不是所有金融工程岗位的必备项。**

如果你对这个方向感兴趣，设计模式值得投入时间，但它只是理解系统架构的一把工具，不是万能钥匙，更代替不了金融业务知识和系统设计能力本身。

## 三、教程的核心：按金融应用频率排的四梯队

这是教程里最重要的一章。22 个 GoF 模式，如果逐个平铺地讲，学习者很容易「学完就忘」，因为不知道哪些值得深入。所以我们按「在金融工程里用得有多频繁」分成了四档：


|  梯队  | 数量 | 定位                     | 典型模式                                                               |
| :------: | :----: | -------------------------- | ------------------------------------------------------------------------ |
| Tier 1 |  5  | 最常用，建议优先掌握     | Strategy、Observer、Factory、Decorator、Singleton                      |
| Tier 2 |  5  | 较常用，构建系统时常遇到 | Command、Builder、Template Method、State、Adapter                      |
| Tier 3 |  5  | 特定场景使用             | Proxy、Bridge、Chain of Responsibility、Composite、Visitor             |
| Tier 4 |  7  | 按需查阅即可             | Mediator、Memento、Iterator、Flyweight、Prototype、Facade、Interpreter |

这个排序的依据，是「一个模式对应金融系统中的哪类常见问题」：

- **Strategy**：算法可以在运行时替换。定价引擎切换 BS / Monte Carlo / PDE 就是典型场景，这类「按需换算法」的问题在金融里很普遍。
- **Observer**：一对多的通知。行情来了要通知多个策略模块去处理，这是事件驱动系统的常见结构。
- **State**：对象状态随事件流转。一笔订单 New → Pending → Filled → Cancelled 的流转，本质上就是状态机。
- **Interpreter**：解析自定义语法。比如自己实现一套交易规则 DSL，属于比较重的工程，日常开发遇到的机会不多，所以放在按需档。

坦率地说，这个排序带有一定的主观判断，是我基于常见金融系统架构的归纳，并非什么权威标准。它的价值在于给你一个「先学什么」的参考，而不是一个必须严格遵守的清单。不同团队、不同业务，优先级很可能不一样。

## 四、为什么教程里专门写了一章「反模式」

我在教程里加了一章「过度引入设计模式的代价」。加这一章，是因为我见过不少（包括我自己早年的）反面例子：学了模式之后，恨不得每个地方都套一个，结果代码变得更难维护。

几个常见的坑：

- **过度工程**：本来一个 `if/else` 或一个 `std::function` 就能解决的策略选择，硬是套上「接口 + 工厂 + 注册表」一套，增加了没必要的复杂度。
- **性能损耗**：在延迟敏感的路径上，多层抽象意味着多次虚函数调用、间接层和内存分配。这些在纳秒级的关键路径上是实打实的成本。
- **维护成本**：金融业务规则变动频繁（监管、品种、风控阈值），模式化程度过高的代码，改起来要动的文件更多，出错的概率也更高。
- **误用模式**：比如用 Singleton 管理可变全局状态，容易引入并发竞态和测试困难。

所以教程里的态度是克制的：**先写直白可读的代码，当重复和变化真正出现、且符合某个模式解决的问题时，再引入模式。** 模式的目的是让业务清晰、系统易改，如果它反而让代码更难懂，那它就是失败的。

这套态度也体现在代码里：除了传统的「接口 + 多态」实现，我们还补充了一个用 `std::function` 实现的轻量版 Strategy，展示「不一定要上模式也能解决同样问题」的另一种思路。

## 五、教程的结构与代码风格

仓库按 GoF 分类组织目录，每个模式独立成目录，并配有 UML 图和多个变体实现。除了三大类模式，还额外增加了并发示例目录和几份配套文档：

```
cpp_design_patterns/
├── SOLID/               # 5 个原则
├── Creational/          # Builder、Factory、Prototype、Singleton
├── Structural/          # Adapter、Bridge、Composite、Decorator、Façade、Flyweight、Proxy
├── Behavioral/          # CoR、Command、Interpreter、Iterator、Mediator、Memento、
│                        # Observer、State、Strategy、Template、Visitor
├── Concurrency/         # 线程安全设计模式的可运行示例（thread_safe_patterns.cpp、
│                        #   multi_strategy.cpp）
├── CMakeLists.txt       # C++17 + CMake，一键构建
├── README.md            # 主教程：四梯队排序、反模式、线程安全精华、模式索引
├── THREAD_SAFETY_GUIDE.md   # 每种设计模式的多方案线程安全详解
└── ZHIHU_ARTICLE.md     # 本文
```

代码风格上，我比较注意几点：

- **使用 Modern C++**：智能指针、`enum class`、范围 for、`override` 等 C++11/17 特性，避免写「上世纪风格」的 C++。
- **坚持 RAII 与资源安全**：能用 `unique_ptr`/`shared_ptr` 就不用裸指针，多态基类补虚析构，避免内存泄漏与未定义行为。
- **每段代码配中文注释**：注释固定包含「一句话概括、适用场景、金融应用、关键参与者」几块，方便快速定位。
- **先演示反面写法**：不少模式先给一个「朴素但有问题的」实现，再重构出正确写法，让读者理解「为什么需要这个模式」。
- **同一模式多变体**：比如动态多态 / 静态模板 / 函数式三种实现，帮助理解不同取舍。
- **并发优先**：新增的 `Concurrency/` 目录专门演示线程安全写法（Meyers' Singleton、快照 Observer、命令队列、多方案对比），并用真实多线程并发验证——这在金融系统里是刚需。

## 六、线程安全：设计模式在并发金融系统里的硬要求

这一章是后来补充的，但我觉得它值得单独讲。因为教程里的很多模式，在**单线程示例**里是「对的」，可一旦放进真实的并发金融系统（行情推送、订单撮合、多策略并行），就会暴露出线程安全问题。

举两个具体的例子：

- **Singleton**：常见的「懒加载 + 手写双重检查」在多线程下是错的，可能读到半初始化对象。教程给出了四种线程安全写法，其中最推荐的是 **Meyers' Singleton**——C++11 标准保证函数内 `static` 局部变量的初始化是线程安全的，一行代码解决，无需手写任何锁。
- **Observer**：观察者的「订阅 / 退订 / 通知」是经典的并发难题。通知时另一个线程退订，会导致迭代器失效；在持锁时调用用户回调，又可能死锁。教程给出的标准做法是**快照通知**——先复制观察者列表、释放锁、再逐个回调。

我在梳理时发现，所有设计模式的线程安全实现，本质上逃不出**五种策略**：


| 策略                   | 核心思想                              | 适用                                    |
| ------------------------ | --------------------------------------- | ----------------------------------------- |
| **A. 无状态 / 不可变** | 对象不持有可变状态，或构造后不可变    | Template、Visitor、Memento、Interpreter |
| **B. 锁**              | `mutex` / `shared_mutex` 保护共享状态 | Factory、Mediator、Composite            |
| **C. 原子操作**        | 用`atomic` 原子更新单个指针/整数      | Strategy、State、Singleton              |
| **D. 快照 / 写时复制** | 读拿快照、释放锁再处理                | Observer、Mediator、Iterator            |
| **E. 线程本地 / 隔离** | 每线程独立实例，不共享                | Builder、Prototype、Chain               |

一个很关键的认识是：**同一模式在不同负载下，应该选不同的策略**，没有「唯一正确」的线程安全实现。比如：

- **读多写少**（如 Factory 注册表、Flyweight 驻留表）→ 用 `shared_mutex`（读并发）或 Copy-on-Write（读无锁）。
- **原子切换单个对象**（如 Strategy 换策略、State 换状态）→ 用 `atomic_load/store(shared_ptr)` 或 `atomic` 指针，无锁。
- **生产-消费**（如 Command 命令队列）→ 用 `condition_variable`，有界队列还能加背压防堆积。
- **必须消灭共享可变状态** → 优先选不可变对象、值语义、`thread_local`，这永远比加锁更优。

这套内容不是空谈，都配了**可编译运行**的示例：`Concurrency/thread_safe_patterns.cpp` 演示 Meyers' Singleton、快照 Observer、命令队列；`Concurrency/multi_strategy.cpp` 直接对比 Singleton 的四种写法、Observer 的两种写法、Strategy 的两种写法，用多线程并发验证。每种模式的多方案完整对比，都整理在教程的 [THREAD_SAFETY_GUIDE.md](./THREAD_SAFETY_GUIDE.md) 里。

我也在 README 里加了**决策步骤**：先看能不能消灭共享状态，再判断是否读多写少，是否切换单对象，是否生产-消费……最后才落到通用的 `mutex`。这个「先消除、再优化、后兜底」的顺序，比直接套一把锁要靠谱得多。

## 七、这套教程怎么用

我的建议是把它当成一份「带注释的代码集」来用，而不是从头到尾读一遍就完：

1. **按四梯队顺序浏览**，先看 Tier 1 的五个模式，结合注释里的金融场景，想一下它们在你关心的系统里可能出现在哪。
2. **动手改**：试着把某个模式的「多态版」改成 `std::function` 版，或者反过来，体会两种写法的差异和适用场景。改完再运行，比光看更有用。
3. **对照真实 C++ 代码**：读完每个模式后，去下面「开源 C++ 金融工程学习材料」里挑一两个真实项目，读它们的核心类结构，观察模式是怎么「被问题逼出来」的——尤其推荐 QuantLib 的 `Instrument + PricingEngine + Observer`，它几乎是 Tier 1 模式的综合样板。
4. **补线程安全**：学完一个模式后，想想「如果它要跑在行情推送、订单撮合这种并发场景下，该怎么改」。去 [THREAD_SAFETY_GUIDE.md](./THREAD_SAFETY_GUIDE.md) 看这个模式的多方案，再用 `multi_strategy.cpp` 跑一遍对比，比单独背一种写法更能建立「并发意识」。
5. **结合业务知识**：设计模式只是理解系统的维度之一。金融工程的真正门槛还包括金融业务逻辑、数据、算法、以及系统整体的设计权衡，这些是代码之外更需要持续积累的。

## 八、开源 C++ 金融工程学习材料

下面列出的都是真实存在的开源项目，**以 C++ 为主**（与本教程同语言，对照起来最直接）。每个项目都标注了它体现的设计模式，方便你学完一个模式后去对应的项目里找「实战样本」。阅读时不求一开始就跑通全部代码，当作模式案例库查阅即可。

### 1. 定价与风控类（C++，与教程最直接相关）


| 项目                                                                     | 描述                                   | 值得关注的设计模式                                   |
| -------------------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------ |
| [QuantLib](https://github.com/lballabio/QuantLib)                        | 衍生品定价与风险管理的事实标准开源库   | Observer、Factory、Strategy、Template Method、Bridge |
| [OpenSourceRisk/Engine (ORE)](https://github.com/OpenSourceRisk/Engine)  | 基于 QuantLib 的开放风险引擎           | 观察如何用模式扩展金融工具与定价引擎                 |
| [QuantLib-Risks](https://github.com/auto-differentiation/QuantLib-Risks) | 为 QuantLib 加自动微分的风险敏感性计算 | 对第三方库的扩展与适配（Adapter 思路）               |

> **重点推荐**：QuantLib 的 `Instrument`（金融工具）+ `PricingEngine`（定价引擎）+ `Observer`（缓存联动刷新）三件套，把「金融工具」「定价算法」「依赖刷新」解耦开，正是 Tier 1 里 Factory / Strategy / Observer 的综合应用。想理解「为什么需要设计模式」，读这个就够了。

### 2. 交易与撮合类（C++，低延迟）


| 项目                                              | 描述                                 | 值得关注的设计模式                                     |
| --------------------------------------------------- | -------------------------------------- | -------------------------------------------------------- |
| [liquibook](https://github.com/zjc5415/liquibook) | OCI 开源的现代 C++ 订单撮合引擎      | Observer、Template Method、Command、状态机             |
| [QuickFIX](https://github.com/quickfix/quickfix)  | 老牌 C++ FIX 协议引擎（FIX 4.0~5.0） | Observer（消息回调）、Factory（消息工厂）、Adapter     |
| [Fix8](https://fix8.org/)                         | 现代 C++ 低延迟 FIX 协议框架         | 观察**性能优先**下的可插拔配置（Factory/Adapter 思路） |

> **重点提示**：低延迟项目是「模式 vs 性能」博弈的绝佳样本。你会看到设计者在**事件驱动架构**和**裸写热点路径**之间如何权衡——这正好呼应教程里「过度引入设计模式的代价」一章。Fix8 等在纳秒级路径上刻意减少抽象层，本身就是一种设计取舍。

### 3. 回测与研究类（C++ 为主）


| 项目                                                         | 描述                               | 值得关注的设计模式                                        |
| -------------------------------------------------------------- | ------------------------------------ | ----------------------------------------------------------- |
| [hikyuu](https://github.com/fasiondog/hikyuu)                | 基于 C++/Python 的极速量化研究框架 | Template Method（回测骨架）、Strategy、Composite、Factory |
| [WonderTrader](https://github.com/wondertrader/wondertrader) | 国内 C++ 实现的量化研发与实盘框架  | 事件引擎（Observer）、State、Command、Adapter             |

> **重点提示**：回测框架模块边界清晰（数据→策略→订单→绩效），最容易「读懂全貌」，是验证你模式理解的好起点。hikyuu、WonderTrader 的核心引擎都是 C++，与教程同语言，对照最顺手。

### 4. 补充：非 C++ 项目（思想同源，值得借鉴）


| 项目                                                        |  语言  | 描述                                 | 值得关注的设计模式                             |
| ------------------------------------------------------------- | :------: | -------------------------------------- | ------------------------------------------------ |
| [exchange-core](https://github.com/mzheravin/exchange-core) |  Java  | 基于 LMAX Disruptor 的高性能撮合核心 | 事件驱动（Observer/Mediator）、Command、状态机 |
| [backtrader](https://github.com/mementum/backtrader)        | Python | 经典回测框架                         | Template Method、Observer、Strategy、Composite |
| [vnpy](https://github.com/vnpy/vnpy)                        | Python | 国内广泛使用的量化交易平台           | 事件引擎（Observer）、State、Command、Adapter  |
| [QuantConnect Lean](https://github.com/QuantConnect/Lean)   |   C#   | 机构级多资产回测引擎                 | Template Method、Strategy、Observer、Factory   |

> 设计模式是语言无关的思想，这些项目虽非 C++，但其架构拆分方式（尤其事件驱动与回测骨架）与 C++ 项目高度同构，可作跨语言对照。

### 5. 进一步阅读材料


| 类型     | 材料                                                  | 说明                                   |
| ---------- | ------------------------------------------------------- | ---------------------------------------- |
| 经典书籍 | *Design Patterns*（GoF, 1994）                        | 设计模式圣经，本教程的理论源头         |
| 现代 C++ | *Modern C++ Design*（Andrei Alexandrescu）            | 策略/模板元编程的高级用法              |
| 低延迟   | *Building Low Latency Applications with C++*（Packt） | 电子交易系统的 C++ 实现，含源码仓库    |
| 领域知识 | 各项目 README 与论文                                  | 设计模式之外，仍需补金融业务与定价数学 |

## 九、一些诚实的局限

最后，说几点这套教程的局限，免得误导：

- **它不是完整的金融系统教学**。它只覆盖了「设计模式」这一小块，不涉及金融产品知识、定价数学、市场微观结构、具体业务逻辑等，这些恰恰是金融工程更重的部分。
- **例子是示意性的**。代码里的金融场景（如 BS 定价、订单状态机）是为了展示「模式能用在哪儿」，数值结果未必精确，也不能直接用于生产。真要写定价代码，还是得啃专业文献和成熟库。
- **优先级排序带主观性**。我尽量基于常见系统来归纳，但不同方向（比如高频交易 vs 衍生品定价 vs 风控）对模式的侧重差别很大，建议按自己的方向再调整。

## 结语

C++ 转金融工程，语言基础只是起点。真正要跨过的，是从「把功能写出来」到「把系统设计好」之间的那道坎。设计模式是这个过程中值得投入的一环，但它远不是全部。

这套教程想提供的，是一个相对务实的参考：**按金融应用场景讲清楚每个模式、排一个大概的学习顺序、并提醒你不要滥用模式。** 如果你看完觉得有用，能少走一些弯路，那它的目的就达到了。

---

*仓库：[cpp_design_patterns](https://github.com/mpavezb/cpp_design_patterns/)（原始来源） · 本教程在此基础上做了金融场景补充与顺序重排*
